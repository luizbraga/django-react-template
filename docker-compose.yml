version: '2'

services:

    nginx:
        extends:
            file: docker-common.yml
            service: nginx
        ports:
            - 8000:8000
        volumes:
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
        volumes_from:
            - webserver

    database:
        extends:
            file: docker-common.yml
            service: database
        ports:
            - 5433:5432

    webserver:
        extends:
            file: docker-common.yml
            service: django
        links:
            - database
        entrypoint:
            - /django-entrypoint.sh
        expose:
            - 8000

    frontend:
        extends:
            file: docker-common.yml
            service: web
        links:
            - webserver
        entrypoint:
            - /web-entrypoint.sh
